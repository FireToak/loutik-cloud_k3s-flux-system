---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: site-portfolio
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: site-portfolio
  policy:
    semver:
      range: '>=1.0.0'
