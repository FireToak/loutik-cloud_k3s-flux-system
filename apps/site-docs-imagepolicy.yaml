---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: site-docs
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: site-docs
  policy:
    semver:
      range: '>=1.0.0'
